# 2026-03-03

## Resumo do dia
- George pediu estudo/implantação de conversa por voz em tempo real no computador usando OpenClaw.
- Houve problema de entrega de áudio no WhatsApp: mensagens com `[[tts:...]]` estavam chegando como texto bruto.
- Ajuste aplicado: para resposta em áudio, gerar arquivo `.opus` com OpenAI TTS (voz masculina) e enviar como voice note via `message.send` (`asVoice: true`).
- Skills avaliadas do ClawHub: `summarize` (prioridade alta), `tavily-search` (plano B), `agent-browser` (redundante no cenário atual), `self-improving-agent` (opcional).
- `summarize` foi instalado e habilitado no ambiente.
- Perplexity foi configurada no OpenClaw e validada em teste com `web_search` retornando provider `perplexity`.
- Diagnóstico YouTube/transcript no cloud: bloqueio anti-bot por IP de datacenter (incluindo falha com `yt-dlp` sem cookies).
- Plano técnico definido para resolver YouTube de forma estável: rota por IP residencial (WireGuard/Tailscale), fallback com cookies do navegador no `yt-dlp`, e Apify como contingência.

## Pedido executado à noite
- George pediu no Dashboard (Prompt Manager) variações de prompt em **Forró > Forró Emocional**.
- Foram geradas 10 variações de estilo e enviadas em áudio (seguindo regra áudio→áudio).
- Correção solicitada por George: conteúdo copiável (URL, código, prompt) **nunca** deve ser enviado por áudio; deve ir em texto, mesmo quando a mensagem de origem for áudio.
- Regra ampliada no fim da noite: pedidos de mensagem/prompt/texto para uso prático devem ser respondidos em texto sempre; áudio apenas para conversa.
