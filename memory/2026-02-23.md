# 2026-02-23 - Notas da Sessão

## Testes de Skills

### youtube-transcript
- Skill funcional, mas **requer WireGuard VPN configurado**
- Script checa automaticamente se WireGuard está ativo
- Sem VPN, não consegue conectar ao YouTube (IP de cloud bloqueado)

### gemini
- Binário instalado em `/usr/bin/gemini`
- **Precisa de `GEMINI_API_KEY`** nas env vars ou em `/root/.gemini/settings.json`
- Alternativas: GOOGLE_GENAI_USE_VERTEXAI ou GOOGLE_GENAI_USE_GCA

### weather
- Testado com `wttr.in/Piripiri`
- Travou (possível bloqueio de IP ou rede lenta)

## Status
- Skills em `/usr/lib/node_modules/clawdbot/skills/` e `/root/clawd/skills/`
- GEO Dashboard funcionando normalmente
- Heartbeats operacionais

## Gemini status de hoje
- Gemini está instalado (versão 0.26.0) em /usr/bin/gemini
- WireGuard não ativo
- GEMINI_API_KEY ausente; settings.json não contém chave válida
- Sem chave, Gemini não funciona; fallback ativo: uso de Modal/OpenAI (GLM-5 FP8) para tarefas que seriam alimentadas pelo Gemini
- Ações seguintes: fornecer GEMINI_API_KEY ou instruções para configurar o settings.json e ativar Gemini. Enquanto isso, seguirei com o fallback.

### Observação durável (Gemini offline)
- Gemini não funcionando: GEMINI_API_KEY ausente; WireGuard não ativo
- Fallback ativo: GLM-5 FP8 Modal/OpenAI para tarefas Gemini
- Próximos passos: inserir GEMINI_API_KEY no /root/.gemini/settings.json; ativar WireGuard; se chave não fornecida, continuar com fallback.
